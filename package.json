{
  "name": "datum",
  "version": "1.6.3",
  "description": "Datum - a fully offline GIS-style topo/satellite/trail map with GPS, waypoints, and route recording",
  "scripts": {
    "sync": "npx cap sync android",
    "fix-manifest": "node scripts/ensure-manifest-permissions.js && node scripts/ensure-storage-plugin.js && node scripts/ensure-compass-plugin.js && node scripts/ensure-gpx-intent.js && node scripts/ensure-branding.js && node scripts/ensure-version.js && node scripts/ensure-signing.js",
    "open:android": "npx cap open android",
    "add:android": "npx cap add android",
    "build:android": "node scripts/prepare-android.js"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/app": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/filesystem": "^6.0.0",
    "@capacitor/geolocation": "^6.0.0",
    "@capacitor/preferences": "^6.0.0",
    "leaflet": "^1.9.4"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
